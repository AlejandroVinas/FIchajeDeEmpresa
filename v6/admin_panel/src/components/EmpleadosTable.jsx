import { useState } from 'react';

export default function EmpleadosTable({ empleados = [], supervisores = [], onUpdate, onDelete }) {
  const [editingId, setEditingId] = useState(null);
  const [draft, setDraft] = useState({});
  const [busyId, setBusyId] = useState(null);
  const [error, setError] = useState('');

  function startEdit(e) { setError(''); setEditingId(e.id); setDraft({ nombre: e.nombre || '', email: e.email || '', password: '', pin: '', role: e.role || 'empleado', supervisor_id: e.supervisor_id || '', activo: e.activo !== false, horas_jornada: e.horas_jornada ?? 8, hora_entrada: e.hora_entrada || '09:00', hora_salida: e.hora_salida || '17:00', horas_semanales: e.horas_semanales ?? 40 }); }
  function cancelEdit() { setEditingId(null); setDraft({}); setError(''); }
  function updateDraft(field, value) { setDraft((prev) => ({ ...prev, [field]: value })); }
  async function saveEdit(id) { setBusyId(id); setError(''); try { const payload = { ...draft, supervisor_id: draft.supervisor_id || null, horas_jornada: Number(draft.horas_jornada || 8), horas_semanales: Number(draft.horas_semanales || 40) }; if (!payload.password) delete payload.password; if (!payload.pin) delete payload.pin; await onUpdate(id, payload); cancelEdit(); } catch (err) { setError(err.message || 'No se pudo actualizar'); } finally { setBusyId(null); } }
  async function deleteEmpleado(e) { if (!window.confirm(`Â¿Eliminar a ${e.nombre}?`)) return; setBusyId(e.id); try { await onDelete(e.id); } catch (err) { setError(err.message || 'No se pudo eliminar'); } finally { setBusyId(null); } }

  return <div className="card overflow-hidden"><div className="border-b px-5 py-4"><h2 className="text-lg font-semibold">Listado de empleados</h2>{error ? <p className="mt-2 text-sm text-red-600">{error}</p> : null}</div><div className="overflow-x-auto"><table className="min-w-full divide-y divide-gray-200 text-sm"><thead className="bg-gray-50"><tr>{['Nombre','Email','Rol','Supervisor','Activo','PIN','Jornada','Horario','Acciones'].map((h) => <th key={h} className="px-4 py-3 text-left font-semibold text-gray-600">{h}</th>)}</tr></thead><tbody className="divide-y divide-gray-100 bg-white">{empleados.map((e) => { const ed = editingId === e.id; return <tr key={e.id}><td className="px-4 py-3 min-w-[180px]">{ed ? <input className="input" value={draft.nombre} onChange={(ev) => updateDraft('nombre', ev.target.value)} /> : <span className="font-medium">{e.nombre}</span>}</td><td className="px-4 py-3 min-w-[220px]">{ed ? <input className="input" type="email" value={draft.email} onChange={(ev) => updateDraft('email', ev.target.value)} /> : e.email}</td><td className="px-4 py-3 min-w-[130px]">{ed ? <select className="input" value={draft.role} onChange={(ev) => updateDraft('role', ev.target.value)}><option value="empleado">Empleado</option><option value="supervisor">Supervisor</option><option value="admin">Admin</option></select> : <span className="rounded-full bg-indigo-50 px-2.5 py-1 text-xs font-semibold text-indigo-700">{e.role}</span>}</td><td className="px-4 py-3 min-w-[180px]">{ed ? <select className="input" value={draft.supervisor_id} onChange={(ev) => updateDraft('supervisor_id', ev.target.value)}><option value="">Sin supervisor</option>{supervisores.filter((s) => s.id !== e.id).map((s) => <option key={s.id} value={s.id}>{s.nombre}</option>)}</select> : (supervisores.find((s) => s.id === e.supervisor_id)?.nombre || 'â€”')}</td><td className="px-4 py-3">{ed ? <input type="checkbox" checked={Boolean(draft.activo)} onChange={(ev) => updateDraft('activo', ev.target.checked)} /> : (e.activo ? 'SÃ­' : 'No')}</td><td className="px-4 py-3 min-w-[130px]">{ed ? <input className="input" value={draft.pin} placeholder={e.tiene_pin ? 'Cambiar PIN' : 'Nuevo PIN'} onChange={(ev) => updateDraft('pin', ev.target.value)} /> : (e.tiene_pin ? 'Configurado' : 'â€”')}</td><td className="px-4 py-3 min-w-[160px]">{ed ? <div className="grid grid-cols-2 gap-2"><input className="input" type="number" value={draft.horas_jornada} onChange={(ev) => updateDraft('horas_jornada', ev.target.value)} /><input className="input" type="number" value={draft.horas_semanales} onChange={(ev) => updateDraft('horas_semanales', ev.target.value)} /></div> : `${e.horas_jornada} h/dÃ­a Â· ${e.horas_semanales} h/sem`}</td><td className="px-4 py-3 min-w-[190px]">{ed ? <div className="grid grid-cols-2 gap-2"><input className="input" type="time" value={draft.hora_entrada} onChange={(ev) => updateDraft('hora_entrada', ev.target.value)} /><input className="input" type="time" value={draft.hora_salida} onChange={(ev) => updateDraft('hora_salida', ev.target.value)} /></div> : `${e.hora_entrada || 'â€”'} - ${e.hora_salida || 'â€”'}`}</td><td className="px-4 py-3 min-w-[220px]">{ed ? <div className="flex gap-2"><button className="btn btn-primary" disabled={busyId === e.id} onClick={() => saveEdit(e.id)}>Guardar</button><button className="btn btn-ghost" onClick={cancelEdit}>Cancelar</button></div> : <div className="flex gap-2"><button className="btn btn-ghost" onClick={() => startEdit(e)}>Editar</button><button className="btn btn-danger" onClick={() => deleteEmpleado(e)}>Eliminar</button></div>}</td></tr>; })}{!empleados.length ? <tr><td colSpan="9" className="px-4 py-8 text-center text-gray-500">No hay empleados.</td></tr> : null}</tbody></table></div></div>;
}