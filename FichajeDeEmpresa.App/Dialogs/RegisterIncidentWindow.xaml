<Window x:Class="FichajeDeEmpresa.App.Dialogs.RegisterIncidentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Registrar incidencia"
        Width="600"
        SizeToContent="Height"
        MinWidth="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource WindowBackgroundBrush}">

    <Grid Margin="18">
        <Border Style="{StaticResource AppCardBorderStyle}"
                Padding="20">
            <StackPanel>
                <TextBlock Text="Registrar incidencia"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryBrush}" />

                <TextBlock x:Name="SelectedUserTextBlock"
                           Margin="0,12,0,0"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextSecondaryBrush}" />

                <TextBlock Margin="0,18,0,6"
                           Text="Fecha"
                           Style="{StaticResource AppFieldLabelStyle}" />

                <DatePicker x:Name="IncidentDatePicker"
                            Style="{StaticResource AppDatePickerStyle}" />

                <TextBlock Margin="0,18,0,6"
                           Text="Comentario"
                           Style="{StaticResource AppFieldLabelStyle}" />

                <TextBox x:Name="CommentTextBox"
                         Height="120"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         VerticalContentAlignment="Top"
                         Padding="12"
                         FontSize="14"
                         Background="White"
                         Foreground="{StaticResource TextPrimaryBrush}"
                         BorderBrush="{StaticResource BorderBrushStrong}"
                         BorderThickness="1" />

                <Border x:Name="MessageBorder"
                        Margin="0,16,0,0"
                        Background="{StaticResource DangerBackgroundBrush}"
                        BorderBrush="{StaticResource DangerBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="12"
                        Visibility="Collapsed">
                    <TextBlock x:Name="MessageTextBlock"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource DangerBrush}"
                               TextWrapping="Wrap" />
                </Border>

                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="2"
                            Width="120"
                            Content="Cancelar"
                            Style="{StaticResource AppSecondaryButtonStyle}"
                            Click="CancelButton_Click" />

                    <Button Grid.Column="4"
                            Width="170"
                            Content="Guardar incidencia"
                            Style="{StaticResource AppPrimaryButtonStyle}"
                            Click="SaveButton_Click" />
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>